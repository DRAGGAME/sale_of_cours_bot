name: TelegramSaleCourse

channels:
  - conda-forge
  - defaults

dependencies:
  - pip>=21.0.1
  - setuptools>=51.00.0
  - python=3.13.0
  - asyncpg
  - numpy
  - pip:
    - redis>=6.0.0
    - aiogram>=3.7.0
    - python-dotenv

